//
//  9_2_2.c
//  basic_c programming
//
//  Created by 전혜준 on 12/29/24.
//

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/* void printary(int data[], int size);
void increment(int data[], int n, int size);

int main(void) {
    
    int data[] = {4, 7, 2, 3, 5};
    int aryLength = sizeof(data) / sizeof(int);
    int inc = 3;

    printary(data, aryLength); // 배열 출력을 위해 함수 호출
    increment(data,inc, aryLength); // 각 배열 원소를 3씩 증가시키기 위해 함수 호출
    printf("배열 원소에 각각 %d를 더한 결과: \n", inc);
    printary(data, aryLength);
    
    return 0;
}

void printary(int data[], int size) {
    for(int i = 0; i < size; i++) {
        printf("%2d ", data[i]);
    }
    printf("\n");
}

void increment(int data[], int n, int size) {
    for(int i = 0; i < size; i++) {
        data[i] += n;
    }
} */
