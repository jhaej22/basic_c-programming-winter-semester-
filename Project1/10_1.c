//
//  10_1.c
//  basic_c programming
//
//  Created by 전혜준 on 12/29/24.
//

#include <stdio.h>

/* void sub(int param); // 함수 원형

int main(void) {
    
    int n = 10; // n은 지역변수
    printf("%d\n", n);
    
    for(int m = 0, sum = 0; m < 3; m++) {
        sum += m;
        printf("\t%d %d\n", m, sum);
    } // 지역변수 m, sum은 for문 내부에서만 사용가능한 블록 지역변수
    
    printf("%d\n", n); // n은 참조 가능
    // printf("%d %d\n", m, sum); // m, sum은 참조 불가능
    
    sub(20);
    
    return 0;
}

void sub(int param) {
    int local = 100;
    printf("\t%d %d\n", param, local); // param, local 참조 가능
    // printf("%d", n); // n은 참조 불가능
} */
