#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/* int main(void) {

  int i, sum, max;
  printf("양의 정수를 입력하세요: ");
  scanf("%d", &max);

  // for문 이용
  for(i = 1; i <= max; i++) {
    sum += i;
  }
  printf("for문을 이용한 1에서 %d까지의 합: %d\n", max, sum);

  // while문 이용
  i = 1, sum = 0;
  while(i <= max) {
    sum += i;
    i++;
  }
  printf("while문을 이용한 1에서 %d까지의 합: %d\n", max, sum);

  return 0;
} */