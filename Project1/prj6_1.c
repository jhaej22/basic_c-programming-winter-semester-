//
//  prj6_1.c
//  basic_c programming
//
//  Created by 전혜준 on 12/30/24.
//

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/* void in(void);
void out(void);

int g = 10; // 전역 변수
static int sg = 20; // 정적 전역변수

int main(void) {
    
    int a = 100; // main 함수의 자동 지역변수
    
    printf("%d %d %d\n", g, sg, a);
    in(); out();
    in(); out();
    in(); out();
    printf("%d %d %d\n", g, sg, a);
    
    
    return 0;
}

void in(void) {
    
    int fa = 1; // in 함수의 자동 지역변수
    static int fs ; // in 함수의 정적 지역변수
    
    printf("\t%d %d %d %d\n", ++g, ++sg, ++fa, ++fs);
} */
