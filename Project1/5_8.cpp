#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/* int main(void) {

	int a = 0, b = 0;

	printf("두 정수 입력: ");
	scanf("%d %d", &a, &b);

	printf("max value: %d ", (a > b) ? a : b);
	printf("min value: %d\n", (a < b) ? a : b);

	printf("abs of a: %d ", (a > 0) ? a : -a);
	printf("abs of b: %d", (b > 0) ? b : -b);

	return 0;
} */