#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/* int main(void) {

  int input[20];

  printf("배열에 저장할 정수를 여러 개 입력하세요.");
  printf("0을 입력하면 프로그램을 종료합니다.\n");
  int i = 0;
  
  do {
    scanf("%d", &input[i]);
  } while(input[i++] != 0);

  i = 0;
  while(input[i] != 0) {
    printf("%d ", input[i++]);
  }
  printf("");

  return 0;
} */