//
//  9_2.c
//  basic_c programming
//
//  Created by 전혜준 on 12/29/24.
//

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/* int add(int a, int b); // 함수 원형

int main(void) {

  int a = 3, b = 5;
  
  int sum = add(a, b);
  printf("합: %d\n", sum);

  return 0;
}

// 함수 add 구현
int add(int a, int b) {
  int sum = a + b;
  return sum;
}

// main 함수에서 호출이 없으면 findMin 함수 구현은 실행되지 않음
int findMin(int x, int y) {
  int min = x < y ? x : y;
  return min;
} */
