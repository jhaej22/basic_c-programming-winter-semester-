#include <stdio.h>

/*int main() {
	
	int s = 90;
	int x = 1, y = 2, z = 3;

	printf("%d ", 60 <= s && s <= 80 + 20);
	printf("%d ", (x % 2 == 0) ? (y + z) : (y * z));
	printf("%d ", s += ((++x) && (y - 2)));
	return 0;
} */