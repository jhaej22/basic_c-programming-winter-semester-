//
//  lab3.c
//  basic_c programming
//
//  Created by 전혜준 on 12/30/24.
//

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

// 전역 변수
/* int total = 10000;

// 함수 원형
void save(int);
void withdraw(int);


int main(void) {
    
    printf(" 입금액  출금액  총입금액    잔고\n");
    printf("==========================\n");
    printf("%46d\n", total);
    save(50000);
    withdraw(30000);
    save(60000);
    withdraw(20000);
    printf("==========================\n");

    return 0;
}

void save(int money) {
    // 총입금액이 저장되는 정적 지역변수
    static int amount;
    total += money;
    amount += money;
    
    printf("%7d %17d %20d\n", money, amount, total);
}

void withdraw(int money) {
    //총출금액이 저장되는 정적 지역변수
    static int amount;
    total -= money;
    amount += money;
    printf("%15d %20d %9d\n", money, amount, total);
} */
