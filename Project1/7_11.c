#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/* int main(void) {
  int input;
  while(1) {
    printf("양의 정수 또는 0(종료) 입력: ");
    scanf("%d", &input);
    if(input == 0) {
      break;
    }
    printf("입력한 정수 %d: 16진수로는 %#x입니다.\n", input, input);
  }
  puts("프로그램을 종료합니다.");

  return 0;
} */